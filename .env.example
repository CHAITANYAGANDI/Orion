# Copy to .env and fill in. Defaults let the whole stack run WITHOUT any real keys
# (auth in dev mode, AI in mock mode). Add real keys to enable full features.

# ---- Auth ----
# dev  = accept X-Dev-User header (no Clerk needed)
# clerk = validate real Clerk JWTs (requires CLERK_ISSUER + CLERK_JWKS_URL)
RECALLIX_AUTH_MODE=dev
NEXT_PUBLIC_AUTH_MODE=dev
CLERK_ISSUER=
CLERK_JWKS_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Shared secret for FastAPI -> Spring internal callbacks
RECALLIX_INTERNAL_TOKEN=dev-internal-token

# ---- AI provider (summaries, extraction, RAG) ----
# mock   = deterministic fake transcript/summary (no key, great for demos/tests)
# openai = real Whisper + GPT
AI_PROVIDER=mock
OPENAI_API_KEY=
OPENAI_TRANSCRIBE_MODEL=whisper-1
OPENAI_CHAT_MODEL=gpt-5.6-terra
OPENAI_EXTRACTION_MODEL=gpt-5.6-luna

# ---- Transcription provider (chosen separately from AI_PROVIDER) ----
# auto     = follow AI_PROVIDER (default; what every earlier setup expects)
# mock     = scripted transcript, no key
# openai   = Whisper. Accurate, but does NOT diarize — every speaker is "S1".
# deepgram = real speaker separation ("Speaker 1", "Speaker 2", …)
#
# Deepgram for speech + mock for analysis is a useful combination: it exercises
# the real audio path and real diarization while costing nothing for the LLM.
TRANSCRIPTION_PROVIDER=auto

# assemblyai is the default choice for meetings: better speaker-attributed
# accuracy than Deepgram, and no 25MB chunking, so speaker identity holds
# across a long recording rather than restarting partway through.
ASSEMBLYAI_API_KEY=
ASSEMBLYAI_MODEL=universal-3-5-pro
# `speech_models` is a priority list: this is routed to when the detected
# language is not supported by the model above. Blank disables the fallback.
ASSEMBLYAI_FALLBACK_MODEL=universal-2
# Blank = auto-detect the language. Set an ISO code (e.g. es) when every
# meeting is in one language; detection is good but a wrong guess is costly.
ASSEMBLYAI_LANGUAGE=

# deepgram remains supported; lower latency, slightly weaker on attribution.
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-3
DEEPGRAM_LANGUAGE=

# --- Second-opinion diarization (optional, off) ------------------------------
# Lets an acoustic diarizer overrule the transcription provider's speaker
# labels. Left off: on ground-truth audio pyannote Community-1 is exact, but so
# is AssemblyAI, and on a real phone-call recording it reported silence across
# 21% of the words and split "Yes, sir." between two speakers. The numbers are
# in docs/diarization.md section 12.
#
# Turning it on also needs `pip install pyannote.audio` in the image and an
# account that has accepted the model's terms at
# huggingface.co/pyannote/speaker-diarization-community-1.
DIARIZATION_PROVIDER=none
HF_TOKEN=
